"""
【最长公共子序列 - 记忆化搜索（DP）】

思路（简洁）：
dfs(i, j) 表示 s[0..i] 与 t[0..j] 的最长公共子序列长度。
- 若 i < 0 或 j < 0：某个串为空，返回 0。
- 若 s[i] == t[j]：该字符一定在 LCS 中，返回 dfs(i-1, j-1) + 1。
- 否则：要么丢掉 s[i]，要么丢掉 t[j]，取两者最大值。
用 @cache 记忆化，避免重复计算。

易错点：
1. 递归边界是 i < 0 或 j < 0，返回 0。
2. 字符相同时，需要同时左移 i 和 j。
3. 字符不同时，取 max(dfs(i-1, j), dfs(i, j-1))。
4. @cache 需要 Python 3.9+，低版本用 @lru_cache(None)。
5. 输入字符串可能带 JSON 引号，需兼容处理。

时空复杂度：
- 时间复杂度 O(m * n)，m、n 分别为两个字符串长度。
- 空间复杂度 O(m * n)，记忆化缓存所有状态。

输入输出示例：
示例 1：
输入: abcde ace
输出: 3

示例 2：
输入: abc def
输出: 0

示例 3：
输入: abc abc
输出: 3
"""

import sys
from functools import cache


class Solution:
    def longestCommonSubsequence(self, s: str, t: str) -> int:
        @cache
        def dfs(i: int, j: int) -> int:
            if i < 0 or j < 0:      # 某个字符串空了
                return 0
            if s[i] == t[j]:        # 末尾相同
                return dfs(i - 1, j - 1) + 1
            return max(dfs(i - 1, j), dfs(i, j - 1))  # 末尾不同

        return dfs(len(s) - 1, len(t) - 1)


def main():
    # 输入两个字符串，空格或换行分隔，如：abcde ace
    data = sys.stdin.read().split()
    if len(data) < 2:
        return
    s = data[0].strip('"')
    t = data[1].strip('"')
    sol = Solution()
    print(sol.longestCommonSubsequence(s, t))


if __name__ == "__main__":
    main()

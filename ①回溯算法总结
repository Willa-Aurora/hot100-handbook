什么是回溯算法？
回溯是一种可以反悔的dfs（遍历）。

回溯的三要素：
1.路径（path）：
  放了哪些。
2.选择列表（choices）：
  还可以放哪些。
3.结束条件（end）：
  什么时候算结束。

模板：做选择→递归→撤销选择
  def backtrack(路径，选择列表）:
      if 满足结束条件:
          记录答案
          return

      for 选择 in 选择列表:
          if 选择不合法:
              continue

          做选择
          backtrack(路径，新的选择列表)
          撤销选择

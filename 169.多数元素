"""
【多数元素 - 摩尔投票法】

思路（简洁）：
维护候选元素 x 和票数 votes。
遍历数组：
- 若 votes == 0，则令 x = num（更换候选人）
- 若 num == x，票数 +1；否则票数 -1
最终剩下的 x 就是出现次数超过一半的元素。

易错点：
1. 题目保证多数元素一定存在，因此最后 x 一定有效。
2. votes 初始为 0，第一次遇到元素时将其设为候选。
3. 投票过程相当于“抵消”：不同元素两两抵消，多数元素最终会留下。
4. 输入为空时不会出现（题目保证非空）。

时空复杂度：
- 时间复杂度 O(n)，一次遍历
- 空间复杂度 O(1)，仅用两个变量

输入输出示例：
示例 1：
输入: [3,2,3]
输出: 3

示例 2：
输入: [2,2,1,1,1,2,2]
输出: 2

示例 3：
输入: [1]
输出: 1
"""

import sys
import json
from typing import List


class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        votes = 0
        x = None
        for num in nums:
            if votes == 0:
                x = num
            votes += 1 if num == x else -1
        return x


def main():
    data = sys.stdin.read().split()
    if not data:
        return
    nums = json.loads(data[0])
    sol = Solution()
    print(sol.majorityElement(nums))


if __name__ == "__main__":
    main()

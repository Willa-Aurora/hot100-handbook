"""
【分割回文串 - 回溯 + 双指针】

思路（简洁）：
从左到右扫描字符串，用 start 记录当前子串的起始位置，i 为当前切割点。
对于每个位置 i，有两种选择：
1. 不切割：继续向后扩展（仅当 i < n-1 时，否则最后一段无法继续）。
2. 切割：若 s[start:i+1] 是回文，则加入 path，从 i+1 开始新一段递归。
当 i 到达 n 时，说明整个字符串已分割完，将 path 加入答案。

易错点：
1. 需要同时维护两个指针：i（当前扫描位置）和 start（当前段的起点）。
2. 不切割分支必须在 i < n-1 时才能递归，否则 i 已经到末尾，必须切割或结束。
3. 判断回文可直接用 s[start:i+1] == s[start:i+1][::-1]。
4. path 加入答案前需 copy()，否则后续修改会污染结果。
5. 输入空字符串时，返回 [[]]（一个空分割）。

时空复杂度：
- 时间复杂度 O(n * 2^n)，共 2^(n-1) 种分割方式，每种判断回文 O(n)
- 空间复杂度 O(n)，递归栈和路径长度

输入输出示例：
示例 1：
输入: "aab"
输出: [["a","a","b"],["aa","b"]]

示例 2：
输入: "a"
输出: [["a"]]

示例 3：
输入: ""
输出: [[]]
"""

import sys
import json
from typing import List


class Solution:
    def partition(self, s: str) -> List[List[str]]:
        n = len(s)
        ans = []
        path = []

        def dfs(i: int, start: int) -> None:
            if i == n:
                ans.append(path.copy())
                return

            # 分支1：不切割，继续向后扩展（i < n-1 才有意义）
            if i < n - 1:
                dfs(i + 1, start)

            # 分支2：切割，判断 s[start:i+1] 是否为回文
            t = s[start:i + 1]
            if t == t[::-1]:
                path.append(t)
                dfs(i + 1, i + 1)
                path.pop()

        dfs(0, 0)
        return ans


def main():
    data = sys.stdin.read().strip()
    if not data:
        return
    # 输入可能是带引号的 JSON 字符串，也可能是裸字符串
    try:
        s = json.loads(data)
    except:
        s = data
    sol = Solution()
    result = sol.partition(s)
    print(json.dumps(result))


if __name__ == "__main__":
    main()

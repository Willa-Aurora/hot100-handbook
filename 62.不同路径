"""
【不同路径 - 动态规划（滚动数组）】

思路（简洁）：
f[j] 表示到达当前行第 j 列的路径数。
初始化 f[1] = 1（起点），其余为 0。
按行遍历，每行从左到右执行 f[j+1] += f[j]，
相当于 dp[i][j] = dp[i-1][j] + dp[i][j-1]（上一行 + 左边）。
最终 f[n] 即为到达右下角的路径数。

易错点：
1. 数组长度是 n+1，使用 1-based 下标，f[0] 作为哨兵始终为 0。
2. f[1] = 1 表示起点（第 0 行第 0 列）只有 1 条路径。
3. 内层循环必须从左到右，才能利用本行左边已更新的值。
4. 外层循环执行 m 次，代表 m 行。
5. 输入 m、n 均为正整数。

时空复杂度：
- 时间复杂度 O(m * n)
- 空间复杂度 O(n)，一维滚动数组

输入输出示例：
示例 1：
输入: 3 7
输出: 28

示例 2：
输入: 3 2
输出: 3

示例 3：
输入: 7 3
输出: 28
"""

import sys


class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        f = [0] * (n + 1)
        f[1] = 1
        for _ in range(m):
            for j in range(n):
                f[j + 1] += f[j]
        return f[n]


def main():
    data = sys.stdin.read().split()
    if len(data) < 2:
        return
    m, n = int(data[0]), int(data[1])
    sol = Solution()
    print(sol.uniquePaths(m, n))


if __name__ == "__main__":
    main()

"""
【最长回文子串 - 中心扩展法】

思路（简洁）：
枚举所有可能的回文中心，共 2n-1 个（n 个奇数中心，n-1 个偶数中心）。
对每个中心向两边扩展，直到字符不相等或越界。
记录扩展过程中得到的最长回文子串的左右边界，最终返回该子串。

用 i 从 0 到 2n-2 遍历：
- 左边界 l = i // 2
- 右边界 r = (i + 1) // 2
这样 i 为偶数时 l == r（奇数长度中心），i 为奇数时 r = l + 1（偶数长度中心）。

易错点：
1. 中心总数是 2n-1，不是 2n。
2. 循环结束后，有效回文区间是 [l+1, r-1]，长度为 r-l-1。
3. 更新答案时用长度比较：r - l - 1 > ans_right - ans_left。
4. 返回时切片 s[ans_left:ans_right]（右开区间）。
5. 输入可能带 JSON 引号，需要兼容。

时空复杂度：
- 时间复杂度 O(n^2)，每个中心最多扩展 O(n)
- 空间复杂度 O(1)，仅用常数变量

输入输出示例：
示例 1：
输入: "babad"
输出: "bab"   （"aba" 也是正确答案）

示例 2：
输入: "cbbd"
输出: "bb"

示例 3：
输入: "a"
输出: "a"
"""

import sys


class Solution:
    def longestPalindrome(self, s: str) -> str:
        n = len(s)
        ans_left = ans_right = 0

        for i in range(2 * n - 1):
            l, r = i // 2, (i + 1) // 2
            while l >= 0 and r < n and s[l] == s[r]:
                l -= 1
                r += 1
            # 循环结束后，s[l+1] 到 s[r-1] 是回文串
            if r - l - 1 > ans_right - ans_left:
                ans_left, ans_right = l + 1, r

        return s[ans_left: ans_right]


def main():
    data = sys.stdin.read().strip()
    if not data:
        return
    # 去掉可能的 JSON 引号
    if data.startswith('"') and data.endswith('"'):
        s = data[1:-1]
    else:
        s = data
    sol = Solution()
    print(sol.longestPalindrome(s))


if __name__ == "__main__":
    main()
